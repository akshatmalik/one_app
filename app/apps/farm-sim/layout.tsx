export const metadata = {
  title: 'Farm Sim',
  description: 'A turn-based farming optimization puzzle',
};

export default function FarmSimLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
